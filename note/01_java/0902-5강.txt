0902-6강
1.메소드란? 
-작업을 수행하기 위한 명령문의 집합으로
어떤 값을 입력받아서 처리하고 그 결과를 돌려준다 
하지만 입력받는 값이 없을 수도 있고 결과를 돌려주지 않을 수도 있다.

접근제한자 [static] 리턴type 메소드명([매개변수1, 매개변수2,..]){ 
//return이 없을 경우 리턴type은 void
	처리할 프로세스들;
	[return 리턴값;]
}
2.메소드의 한계
-메소드를 이용하여 로직을 재사용하여 절차 지향 언어에서 중복된 로직을 보다 효율적이게 개발
하지만 문서내에 메소드가 많아질경우 유지보수에 많은 어려움이 발생

3. 객체와  클래스
-객체란? 관련성 있는 데이터와 메소드가 함께있는 구성체
-클래스는 객체를 만들기 위한 설계도!
-클래스를 정의하고 클래스로부터 객체가 생성된다.
-객체 데이터 변수는 값이 아닌 참조값을 저장

4.클래스 제작
package com.lec.ex;
	public class ExClass {
		private 자료형 인스턴스변수( = 속성 = 필드)명;
		public ExClass(){ 	}
		public method(){
				. . .
	}
}
5. 객체지향 프로그램의 특징
캡슐화와 데이터 은닉
다형성: 같은 모습이지만 다른 기능 cf.오버로딩
상속